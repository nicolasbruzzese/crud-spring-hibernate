<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:tx="http://www.springframework.org/schema/tx"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="
        http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context.xsd
        http://www.springframework.org/schema/mvc
        http://www.springframework.org/schema/mvc/spring-mvc.xsd
        http://www.springframework.org/schema/tx
        http://www.springframework.org/schema/tx/spring-tx.xsd">

    <!-- =========================================== -->
    <!-- Escaneo de componentes (@Controller, @Service, @Repository) -->
    <!-- =========================================== -->
    <context:component-scan base-package="com.nicolas" />

    <!-- =========================================== -->
    <!-- Habilitar anotaciones MVC y validaciones -->
    <!-- =========================================== -->
    <mvc:annotation-driven />

    <!-- =========================================== -->
    <!-- Configuración de vistas JSP -->
    <!-- =========================================== -->
    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/view/" />
        <property name="suffix" value=".jsp" />
    </bean>

    <!-- =========================================== -->
    <!-- Configuración de la base de datos y connection pool (C3P0) -->
    <!-- =========================================== -->
    <bean id="myDataSource" class="com.mchange.v2.c3p0.ComboPooledDataSource" destroy-method="close">
        <property name="driverClass" value="com.mysql.cj.jdbc.Driver" />
        <property name="jdbcUrl" value="jdbc:mysql://localhost:3306/gestionclientes?useSSL=false&amp;serverTimezone=UTC" />
        <property name="user" value="root" />
        <property name="password" value="" />

        <!-- Configuración del pool de conexiones -->
        <property name="minPoolSize" value="5" />
        <property name="maxPoolSize" value="20" />
        <property name="maxIdleTime" value="30000" />
    </bean>

    <!-- =========================================== -->
    <!-- Configuración de Hibernate SessionFactory -->
    <!-- =========================================== -->
    <bean id="sessionFactory" class="org.springframework.orm.hibernate5.LocalSessionFactoryBean">
        <!-- Fuente de datos -->
        <property name="dataSource" ref="myDataSource" />

        <!-- Paquete donde Hibernate buscará las entidades (@Entity) -->
        <property name="packagesToScan" value="com.nicolas.entidades" />

        <!-- Propiedades de Hibernate -->
        <property name="hibernateProperties">
            <props>
                <!-- Dialecto para MySQL -->
                <prop key="hibernate.dialect">org.hibernate.dialect.MySQLDialect</prop>

                <!-- Mostrar SQL en consola -->
                <prop key="hibernate.show_sql">true</prop>

                <!-- Opcional: formatear SQL -->
                <prop key="hibernate.format_sql">true</prop>
            </props>
        </property>
    </bean>

    <!-- =========================================== -->
    <!-- Configuración del Transaction Manager de Hibernate -->
    <!-- =========================================== -->
    <bean id="miTransactionManager" class="org.springframework.orm.hibernate5.HibernateTransactionManager">
        <property name="sessionFactory" ref="sessionFactory"/>
    </bean>

    <!-- Habilitar anotaciones @Transactional en DAOs o servicios -->
    <tx:annotation-driven transaction-manager="miTransactionManager" />

    <!-- =========================================== -->
    <!-- Configuración de recursos estáticos (CSS, JS, imágenes) -->
    <!-- =========================================== -->
    <mvc:resources location="/resources/" mapping="/resources/**" />

</beans>
